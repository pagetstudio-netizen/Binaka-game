kind = "web"
previewPath = "/"
title = "BINAKA GAME"
version = "1.0.0"
id = "artifacts/binaka-game"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25528

[services.development]
run = "pnpm --filter @workspace/binaka-game run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/binaka-game", "run", "build" ]
publicDir = "artifacts/binaka-game/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25528"
BASE_PATH = "/"
